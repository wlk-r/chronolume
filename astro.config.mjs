// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://wlk-r.github.io',
  base: '/chronolume',
  output: 'static',
});
